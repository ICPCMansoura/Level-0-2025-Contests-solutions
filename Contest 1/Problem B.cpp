#include <iostream>

using namespace std;
#define int long long
#define endl '\n'

int32_t main() {
    ios::sync_with_stdio(false); cin.tie(0);
	cout.tie(0);
	string s; 
	cin >> s;
	cout << (s == "208241" ? "Yes" : "No");
	return 0;
}
